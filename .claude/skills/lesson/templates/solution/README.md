# Lesson LESSON_NN solutions

Try the exercises before reading these.

Layout mirrors the package: C++ in `src/`, Python in `PACKAGE_NAME_solution/`
(installed as that module), entry scripts in `scripts/`. Everything here is
linted with the lesson; it is only built when you ask:

```bash
colcon build --symlink-install --packages-select PACKAGE_NAME --cmake-args -DTUT_BUILD_SOLUTIONS=ON
```

## Exercise 1

What the solution does and the one thing worth noticing in it.

## Exercise 2

...
