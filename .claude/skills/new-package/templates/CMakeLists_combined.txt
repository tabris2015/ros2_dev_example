cmake_minimum_required(VERSION 3.8)
project(PACKAGE_NAME)

if(NOT CMAKE_CXX_STANDARD)
  set(CMAKE_CXX_STANDARD 17)
endif()

if(CMAKE_COMPILER_IS_GNUCXX OR CMAKE_CXX_COMPILER_ID MATCHES "Clang")
  add_compile_options(-Wall -Wextra -Wpedantic)
endif()

# ---------- Dependencies ----------
find_package(ament_cmake REQUIRED)
find_package(ament_cmake_python REQUIRED)
find_package(rclcpp REQUIRED)
find_package(std_msgs REQUIRED)

# ---------- C++ executables ----------
# add_executable(node_name_cpp src/node_name.cpp)
# target_link_libraries(node_name_cpp rclcpp::rclcpp ${std_msgs_TARGETS})
# install(TARGETS node_name_cpp DESTINATION lib/${PROJECT_NAME})

# ---------- Python module ----------
ament_python_install_package(${PROJECT_NAME})

# install(PROGRAMS
#   scripts/node_name_py
#   DESTINATION lib/${PROJECT_NAME}
# )

# ---------- Testing ----------
if(BUILD_TESTING)
  find_package(ament_lint_auto REQUIRED)
  # Source files carry a one-line copyright, not the full license block
  # ament_copyright wants. cpplint still checks that the line exists.
  set(ament_cmake_copyright_FOUND TRUE)
  ament_lint_auto_find_test_dependencies()

  find_package(ament_cmake_pytest REQUIRED)
  ament_add_pytest_test(pytest_copyright test/test_copyright.py)
  ament_add_pytest_test(pytest_flake8 test/test_flake8.py)
  ament_add_pytest_test(pytest_pep257 test/test_pep257.py)
endif()

ament_package()
